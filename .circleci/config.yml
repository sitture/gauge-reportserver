version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.12
    steps:
      - checkout
      - run:
          name: Get all dependencies
          command: go get ./...
      - run:
          name: Run Tests
          command: go test ./...
  deploy:
    docker:
      - image: maven:3-jdk-8-alpine
    steps:
      - checkout

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build
      - deploy:
          filters:
            tags:
              only: /.*/
            branches:
              ignore: /.*/